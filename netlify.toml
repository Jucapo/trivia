# Netlify config for trivia client (Angular SPA)
[build]
  base    = "client"
  command = "npm run build"
  publish = "dist/client"

# SPA: serve index.html for all routes so Angular router handles /host and /play
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
